/**
 * Page-object registry.
 *
 * Export every page here so tests and fixtures have one import path, and so
 * the `pages` fixture can construct them all in one place.
 */
export { TemplatePage } from './template.page';
export * from './bank';
